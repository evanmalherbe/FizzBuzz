@model List<FizzResult>

<div>
    <h1 class="display-4">Welcome to FizzBuzz!</h1>

    @foreach (var item in Model)
    {
        <span>@item.Word@(item.Count == Model.Count ? "." : ", ")</span>
    }
        
</div>   
